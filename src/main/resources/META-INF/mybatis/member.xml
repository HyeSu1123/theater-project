<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.idev.mapper.MemberMapper">
	<select id="selectAll" resultType="Member">
	select * from members
	</select>

	<!-- 회원 로그인-->
	<select id="login" resultType="Member" >
	select id,password,authority from members where id=#{id} AND authority='1'
	</select>
	<!-- 관리자 로그인-->
	<select id="loginAdmin" resultType="Member" >
	select id,password,authority from members where id=#{id} AND authority='0'
	</select>
	
<!--  암호화 전 
 	<select id="login" resultType="Member" parameterType="java.util.HashMap">
	select * from members where id=#{id} and password=#{password} AND authority='1'
	</select> -->
		
	<!-- 관리자 로그인 
	<select id="loginadmin" resultType="Member" parameterType="java.util.HashMap">
	SELECT * FROM members where id=#{id} and password=#{password} AND authority='0'
	</select>-->
	
	<!-- 중복확인 -->
	<select id="checkid" resultType="int" parameterType="String">
	select count(*) from members where id=#{id}
	</select>
	
	<!-- 회원용 회원가입 -->
	<insert id="insert" parameterType="Member">
	INSERT INTO MEMBERS
	(ID, PASSWORD, NAME, BIRTH_DATE, GENDER, EMAIL, PHONE,REG_DATE,AUTHORITY)
	VALUES(#{id},#{password}, #{name}, #{birth_date}, #{gender}, #{email}, #{phone},sysdate,'1')
	</insert>
	
	<!-- 회원정보조회 -->
	<select id="findPassword" parameterType="String" resultType="Member">
		select * from members where email=#{email}
	</select>
	
	<!-- 패스워드변경 -->
	<update id="updatePassword">
		update members set password=#{password} where email=#{email}
	</update>
	
	<!-- 관리자용 회원가입 -->
	<insert id="insertAdmin" parameterType="Member">
	INSERT INTO MEMBERS
	(ID, PASSWORD, NAME, BIRTH_DATE, GENDER, EMAIL, PHONE, REG_DATE,AUTHORITY)
	VALUES(#{id},#{password}, #{name}, #{birth_date}, #{gender}, #{email}, #{phone}, sysdate,'0')
	</insert>
	
	<update id="update" parameterType="Member">
	UPDATE MEMBERS
	SET EMAIL=#{email}, PHONE=#{phone}
	WHERE ID=#{id}
	</update>
	
	<delete id="delete" parameterType="Member">
	 delete from members where id=#{id}
	</delete>
	
</mapper> 